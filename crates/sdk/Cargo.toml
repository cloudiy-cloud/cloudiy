[package]
name = "cloudiy-sdk"
version.workspace = true
edition.workspace = true
publish = false          # internal workspace crate — never to crates.io
authors.workspace = true
license.workspace = true
repository.workspace = true
description = "Cloudiy SDK — run GPU jobs on the Cloudiy network from Rust apps and AI agents"

[lib]
name = "cloudiy_sdk"
path = "src/lib.rs"

[dependencies]
cloudiy-common = { path = "../common" }
cloudiy-protocol = { path = "../protocol" }

anyhow.workspace = true
base64.workspace = true
iroh.workspace = true
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true
tracing.workspace = true
uuid.workspace = true

[dev-dependencies]
tokio.workspace = true
